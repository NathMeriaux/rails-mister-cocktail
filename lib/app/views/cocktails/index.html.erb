<h1>Cocktails</h1>

<% @cocktails.each do |cocktail| %>
  <%= link_to cocktail.name, cocktail_path(cocktail), {class: "btn btn-link"} %> <%= link_to "Moderate", cocktail_path(cocktail), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-link" %>
  <br>
<% end %>
<br>
<%= link_to "Add a new cocktail", new_cocktail_path, {class: "btn btn-success"} %>
